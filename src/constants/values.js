const values = {
  cpf: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  name: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  address: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  birthDate: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  gender: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  email: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  fatherName: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  motherName: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  phoneNumber1: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  phoneNumber2: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  phoneNumber3: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  phoneNumber4: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  phoneNumber5: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  cellPhoneNumber1: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  cellPhoneNumber2: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  cellPhoneNumber3: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  cellPhoneNumber4: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  cellPhoneNumber5: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  civilStatus: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  car: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  voterTitle: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  zone: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  section: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  id: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  registrationStatus: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  educationLevel: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  occupation: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  socialClass: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  death: { value: 29 },
  presumedRevenue: { value: 55 },
  restriction: { value: 23 },
  restrictionTwoBureaux: { value: 114 },
  cnpj: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  companyName: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  companyFancyName: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  openingDate: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  branch: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  headquarters: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  cnae: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  presumedBilling: { value: 525 },
  corporateControl: { value: 73 },
  size: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  numberOfAssociates: {
    value: 2,
    discount: quantity => {
      if (quantity > 100000 && quantity <= 800000) return 0.15
      if (quantity > 800000) return 0.5
      return 0
    }
  },
  serasaValidation: {
    value: 4,
    discount: quantity => {
      if (quantity > 10000 && quantity <= 50000) return 0.125
      if (quantity > 50000 && quantity <= 100000) return 0.15
      if (quantity > 100000 && quantity <= 200000) return 0.375
      if (quantity > 200000 && quantity <= 400000) return 0.5
      if (quantity > 400000 && quantity <= 800000) return 0.625
      if (quantity > 800000) return 0.75
      return 0
    }
  }
}

export const calculateDiscount = (attribute, quantity) => {
  const { value, discount } = values[attribute]
  const totalValue = discount ? value - value * discount(quantity) : value
  console.log(totalValue, attribute)
  return totalValue
}

export default values
